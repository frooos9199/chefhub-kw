# 📦 دليل تثبيت المكتبات المطلوبة

## المكتبات الأساسية

المشروع يحتاج المكتبات التالية للإشعارات:

### 1. SendGrid (لإرسال الإيميلات)

```bash
npm install @sendgrid/mail
```

**الاستخدام:** تم تفعيله في `lib/email.ts`

### 2. Twilio (لإرسال WhatsApp)

```bash
npm install twilio
```

**الاستخدام:** تم تفعيله في `lib/whatsapp.ts`

---

## التثبيت الكامل

لتثبيت جميع المكتبات المطلوبة مرة واحدة:

```bash
npm install @sendgrid/mail twilio
```

---

## التحقق من التثبيت

بعد التثبيت، تحقق من `package.json`:

```json
{
  "dependencies": {
    "@sendgrid/mail": "^7.x.x",
    "twilio": "^4.x.x"
  }
}
```

---

## ملاحظات مهمة

1. ⚠️ **المكتبات ستعمل فقط بعد إضافة API Keys في `.env.local`**

2. ⚠️ **بدون API Keys:**
   - ستظهر رسائل تحذير في console
   - لن يتم إرسال الإشعارات فعلياً
   - المشروع سيعمل بشكل طبيعي

3. ✅ **الكود الحالي ذكي:**
   - يتحقق من وجود الإعدادات
   - يعمل بدون الإعدادات (وضع DEBUG)
   - لا يوقف التطبيق إذا فشل الإرسال

---

## الخطوات التالية

1. ثبّت المكتبات:
```bash
npm install @sendgrid/mail twilio
```

2. أضف الإعدادات في `.env.local` (اتبع [ENV_VARIABLES_GUIDE.md](ENV_VARIABLES_GUIDE.md))

3. اختبر الإشعارات (اتبع [NOTIFICATIONS_GUIDE.md](NOTIFICATIONS_GUIDE.md))

---

✅ **تم!** الكود جاهز للعمل فوراً بعد التثبيت والإعدادات
